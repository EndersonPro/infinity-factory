// TikTok public-video resolver. Module wiring and the wasm component export
// are added in later tasks alongside retrieval, payload, and mapping. This
// skeleton exists so the workspace registers the crate and `cargo build
// --workspace` succeeds before any classifier logic lands.